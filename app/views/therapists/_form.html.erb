<%= form_with(model: therapist, local: true) do |form| %>
  <% if therapist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(therapist.errors.count, "error") %> prohibited this therapist from being saved:</h2>

      <ul>
      <% therapist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :therapist_name %>
  </div>

  <div class="field">
    <%= form.label :mobile %>
    <%= form.text_field :mobile, id: :therapist_mobile %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :therapist_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
